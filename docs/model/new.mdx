---
id: new
title: Create New Model
sidebar_label: Create New Model
---

import useBaseUrl from "@docusaurus/useBaseUrl"; // Add to the top of the file below the front matter.
import Link from "@docusaurus/Link";
import Zoom from "react-medium-image-zoom";
import "react-medium-image-zoom/dist/styles.css";

In the **Model** tab, click **New** to start building a new model with the options shown in the screenshot below:

<div style={{ textAlign: "center" }}>
  <Zoom>
    <img
      
      alt="Create a new model"
      src={useBaseUrl("/doc-images/model/new1.png")}
    />
  </Zoom>
  <div class="center"><p>Create a new model</p></div>
</div>

1. [Open a Sample Model](#1-open-a-sample-model) - View a built-in sample ValQ model
1. [Create a Simple Model](#2-create-a-simple-model) - ValQ builds a simple model from the data you import
1. [Create an Advanced Model](#3-create-an-advanced-model) - Build a ValQ model from scratch using the data you import
1. [Import an Advanced Model](#4-import-an-advanced-model) - Advanced users can import an already built existing model

:::note
Before starting to build a ValQ model, the first time users can click the
below the icon in the nav-panel to learn more about the ValQ and watch the tutorials.

<div style={{ textAlign: "center" }}>
  <Zoom>
    <img
      alt="First time users"
      src={useBaseUrl("/doc-images/model/new21.png")}
    />
  </Zoom>
</div>
:::

## 1. Open a Sample Model

The ValQ has 4 in-built model samples in the _Open a Sample Model_ option. Select any of the samples to explore ValQ and it's business cycles.

<div class="center">
  <Zoom>
    <img alt="Sample models" src={useBaseUrl("/doc-images/model/new2.png")} />
  </Zoom>
  <p>Sample Models</p>
</div>

Below is a screenshot that shows sample model data for the _"Mining Industry"_.

<div class="center">
  <Zoom>
    <img
      alt="Mining Industry sample"
      src={useBaseUrl("/doc-images/model/new3.png")}
    />
  </Zoom>
  <p>Mining Industry Sample</p>
</div>

### Before you begin

To build the Simple and Advanced Models, you must <Link to={useBaseUrl("/intro/model#1-import-data-from-excel")}>Import data into Power BI and activate ValQ</Link>.

## 2. Create a Simple Model

A Simple model is a tree that gets built based on the hierarchy of the fields in the Category section for the given Time Period. It gets built at the click of a mouse and does not require any configuration as the levels add up one on top of the other.

> The Calculation Method for a Simple Model is always set to Sum Children

A ValQ tree created using the simple model option gets dynamically updated as we re-order the fields in the Category section or add/remove fields to the Category section and behold a different tree at each change. 

Refer the <a href="https://valq.com/wp-content/uploads/valq-for-microsoft-power-bi-beginners-tutorial.pdf" target="_blank">ValQ Beginner's Tutorial</a> for step-by-step instructions on how to build a Simple Model.

<div class="center">
  <Zoom>
    <img alt="Simple model" src={useBaseUrl("/doc-images/model/new4.png")} />
  </Zoom>
  <p>ValQ Simple Model</p>
</div>

### 2.1 Edit a Simple Model

The nodes on a Simple Model are disabled, and you cannot edit the nodes. This means none of the nodes on the tree can be removed, re-ordered nor can be configured till you <Link to={useBaseUrl('model/new#22-convert-to-advanced-model')}>convert the tree into an Advanced Model</Link>. However, new nodes can be added to the simple model and these new nodes can be configured to link a model from workspace as well.

<div class="center">
  <Zoom>
    <img
      alt="Edit a simple model"
      src={useBaseUrl("/doc-images/model/new11.png")}
    />
  </Zoom>
  <p>Edit a Simple Model</p>
</div>

### 2.2 Convert to Advanced Model

To configure the nodes of a Simple Model, convert the the model to an advanced model by clicking the magic wand icon: 

<div style={{ textAlign: "center" }}>
  <Zoom>
    <img alt="Magic wand" src={useBaseUrl("/doc-images/model/new13.png")} />
  </Zoom>
  <div class="center"><p>Magic Wand</p></div>
</div>

Click **Yes** to confirm.

<div style={{ textAlign: "center" }}>
  <Zoom>
    <img
      alt="Convert to advanced model"
      src={useBaseUrl("/doc-images/model/new14.png")}
    />
  </Zoom>
  <div class="center"><p>Convert to advanced model</p></div>
</div>

You have successfully converted the Simple Model into an Advanced Model. You can now configure all the node levels similar to an Advanced Model.

<div class="center">
  <Zoom>
    <img alt="Advanced model" src={useBaseUrl("/doc-images/model/new15.png")} />
  </Zoom>
  <p>Advanced Model</p>
</div>

## 3. Create an Advanced Model

Use Create an Advanced Model option to build a model on ValQ from scratch manually.

<div style={{ textAlign: "center" }}>
  <Zoom>
    <img
      alt=" Create an Advanced Model"
      src={useBaseUrl("/doc-images/model/new18.png")}
    />
  </Zoom>
</div>

In the below screen; a parent node and two child nodes are added by clicking the **+** button.
The node levels are displayed in the Node Editor pane as shown:

<div class="center">
  <Zoom>
    <img alt="Advanced model" src={useBaseUrl("/doc-images/model/new19.png")} />
  </Zoom>
  <p>Advanced Model</p>
</div>

Now you can go ahead and build the model as per your needs using the Model tab options.

## 4. Import an Advanced Model

> To generate an export file from another model's tree configurations, click **Model > General > <Link to={useBaseUrl("model/general#1-export")}>Export</Link>**.

Go to **Model > New** and click **Import an Advanced Model**.

<div class="center">
  <Zoom>
    <img
      alt="Import an Advanced Model"
      src={useBaseUrl("/doc-images/model/advanced-model.png")}
    />
  </Zoom>
  <p>Import an Advanced Model</p>
</div>

You can either paste the tree configuration data in the text area or **Upload** the exported file.
<div class="center">
  <Zoom>
    <img
      alt="Import an Advanced Model"
      src={useBaseUrl("/doc-images/model/upload-export-file.png")}
    />
  </Zoom>
  <p>Import an Advanced Model</p>
</div>

Click **Finish** to view the model in the ValQ visual:

<div class="center">
  <Zoom>
    <img alt="Advanced model" src={useBaseUrl("/doc-images/model/new24.png")} />
  </Zoom>
  <p>Advanced Model Generated</p>
</div>

