---
id: model_overview
title: Model Tab
sidebar_label: Model Tab
---

import useBaseUrl from "@docusaurus/useBaseUrl"; // Add to the top of the file below the front matter.
import Link from "@docusaurus/Link";
import Zoom from "react-medium-image-zoom";
import "react-medium-image-zoom/dist/styles.css";

In the _Model_ tab you can visualize your data and build models.

:::note
Switch from Power BI's Reading view to Edit view to use ValQ's Model tab.
:::

<div class="center">
  <Zoom>
    <img
      alt="Model screen"
      src={useBaseUrl("/doc-images/model/model-screen.png")}
    />
  </Zoom>
  <p>Model Screen</p>
</div>

The _Model_ screen has 2 sections; namely:

- **Nodes Panel**: This is the sidebar-panel on the left, to edit the model nodes and build the model.
- **Canvas(visual)**: This is the canvas on the right, to visualize the model in tree/table structure.

The _Model_ tab has five other tabs as shown in the screenshot below:

<div style={{ textAlign: "center" }}>
  <Zoom>
    <img
      width="450"
      alt="Model tab options"
      src={useBaseUrl("/doc-images/model/model2.png")}
    />
  </Zoom>
</div>

## New

To start <Link to={useBaseUrl("model/new")}>creating new models</Link> select one of the four options as shown in the screenshot below:

<div class="center">
  <Zoom>
    <img
      alt="Create new models"
      src={useBaseUrl("/doc-images/model/model4.png")}
    />
  </Zoom>
  <p>Select a Sample Model</p>
</div>

## Node

In the <Link to={useBaseUrl("model/node")}>Node</Link> tab you can configure the design and
functionality of the _Nodes_ of a model.

<div class="center">
  <Zoom>
    <img alt="Node option" src={useBaseUrl("/doc-images/model/model3.png")} />
  </Zoom>
  <p>Nodes configuration panel</p>
</div>

## Data series

<Link to={useBaseUrl("model/data-series")}>Data Series</Link> tab allow you
to configure the data settings such as Data Series Manager, Time Aggregation Labels
and Data Sorting.

<div class="center">
  <Zoom>
    <img
      alt="Data Series option"
      src={useBaseUrl("/doc-images/model/model5.png")}
    />
  </Zoom>
  <p>Data Series configurations</p>
</div>

## Settings

In the <Link to={useBaseUrl("model/settings")}>Settings</Link> tab you can configure the
parameters such as **Navigational Panel, Nodes, Canvas, Number Formatting,
Conditional Formatting, Color Theme** and **PopUp screen**.

<div class="center">
  <Zoom>
    <img
      alt="Settings option"
      src={useBaseUrl("/doc-images/model/model6.png")}
    />
  </Zoom>
  <p>Settings nav-panel</p>
</div>

## General

In the <Link to={useBaseUrl("model/general")}>General</Link> tab you can configure
the general settings such as **License Registration** and **Export your model**.

<div class="center">
  <Zoom>
    <img
      alt="General option"
      src={useBaseUrl("/doc-images/model/model7.png")}
    />
  </Zoom>
  <p>General options</p>
</div>

In the upcoming sections, you will learn to build a ValQ model using these options in detail.
