---
id: general
title: General
---
import useBaseUrl from '@docusaurus/useBaseUrl'; // Add to the top of the file below the front matter.
import Link from '@docusaurus/Link';
import Zoom from "react-medium-image-zoom";
import "react-medium-image-zoom/dist/styles.css";

**Model > General** tab allows you to configure the following settings:

1. [Export](#export)

2. [License Registration](#license-registration)

3. [Writeback](#writeback)

## 1. Export

<div style={{textAlign: 'justify'}}>
Using the Export option, you will be able to export the ValQ model as an encoded text. There is also an option to include the navigation
panel settings in the Export File.
</div> <br />

<div style={{textAlign: 'center'}}>
  <Zoom>
<img alt="New Tab" src={useBaseUrl('/doc-images/model/gen1.png')} />
  </Zoom>
</div> <br />

<div style={{textAlign: 'center'}}>
  <Zoom>
<img alt="New Tab" src={useBaseUrl('/doc-images/model/gen4.png')} />
  </Zoom>
</div> <br />

<div style={{textAlign: 'justify'}}>
Download the ValQ model as text file to save it to your local system and this exported file can be shared with other users.
The steps to import from an Export file is covered <Link to={useBaseUrl('/docs/model/new#4-import-an-advanced-model')}>here</Link>.
</div>

## 2. License Registration

You can enter the ValQ license in the Text Box to get the latest features.

<div style={{textAlign: 'center'}}>
  <Zoom>
<img alt="New Tab" src={useBaseUrl('/doc-images/model/gen2.png')} />
  </Zoom>
</div>

## 3. Writeback

ValQ custom visual provides the ability to write scenarios, planning series along with the simulation details to an URL which can collect the response and write it to a database, data warehouse, etc.

This enables planners to integrate ValQ's planning and what-if simulations with their business workflows.

- **Format**- ValQ sends the data in a JSON format to the URL which can be processed easily.

- **Configurable** - User can choose to export the entire model, or just the KPIs and simulated affected Nodes.

<div style={{textAlign: 'center'}}>
  <Zoom>
<img alt="Writeback Architecture" src={useBaseUrl('/doc-images/model/writeback/writeback-architecture.png')} />
  </Zoom>
</div> <br />

*Figure: Writeback architecture*

### How to configure Writeback in ValQ

1. The Writeback settings are available under the **Model > General > Writeback** section.

<div style={{textAlign: 'center'}}>
  <Zoom>
<img alt="ValQ logo" src={useBaseUrl('/doc-images/model/writeback/writeback_settings.png')} />
  </Zoom>
</div> <br />

*Figure: Writeback settings*

The following table lists out the fields:

| Field | Description | Options |
| :--- | :--- | :--- |
| URL | URL of the backend data source to which ValQ will post the data | |
| Button Label | Label message displayed in the Writeback section | |
| Button Text | Text message displayed on the Writeback button | |
| Exporting Nodes | Select the nodes to be exported | |
| | |  <ul><li>All Nodes</li><li>Only KPI and Key Inputs nodes</li><li>Only simulation affected nodes</li></ul>|

*Table: Writeback Properties*

2. This Writeback feature will now be available in the **Simulate > Navigation Panel** section on the runtime.

> Note: The option is only enabled once the Writeback settings are configured in the Model section.

<div style={{textAlign: 'center'}}>
  <Zoom>
<img alt="Writeback feature accessible from the Nav Panel" src={useBaseUrl('/doc-images/model/writeback/navpanel_writeback_icon.png')} />
  </Zoom>
</div>

*Figure: Writeback feature accessible from the Navigation Panel*

  - Click the **Writeback** button

<div style={{textAlign: 'center'}}>
  <Zoom>
<img alt="Writeback button" src={useBaseUrl('/doc-images/model/writeback/series_scenario_select.png')} />
  </Zoom>
</div>

  - Select the *Series* and *Scenarios* that you want to export to the URL and submit.

<div style={{textAlign: 'center'}}>
  <Zoom>
<img alt="writeback-posted-success" src={useBaseUrl('/doc-images/model/writeback/writeback-posted-success.png')} />
  </Zoom>
</div>

> Success: Your modeled scenarios and series have been successfully posted to the external URL.

### ValQ Writeback Schema

Writeback posts JSON to the writeback URL in a specific export schema.

This export format includes a JSON object with the following details:

- **Result** array includes the *Scenarios/Series* fields and the respective *Comments History*

- **Simulated Nodes** array includes fields details related to the change in values of the *Simulated Nodes*

- Logged in **ValQ User details**

You can download the schema file here <a href={useBaseUrl('/assets/jsonSchema.json')} download>ValQ Writeback Schema.json</a>

<div style={{textAlign: 'center'}}>
  <Zoom>
<img alt="writeback-format" src={useBaseUrl('/doc-images/model/writeback/writeback-format.png')} />
  </Zoom>
</div>

*Figure: Writeback Schema format*

### Writeback History

History of the Writeback logs is maintained and is accessed by clicking on the history icon right above the Writeback button in the Navigation panel.

<div style={{textAlign: 'center'}}>
  <Zoom>
<img alt="writeback-history" src={useBaseUrl('/doc-images/model/writeback/writeback_history_logs.png')} />
  </Zoom>
</div>

*Figure: Writeback History Logs*

## Summary - General Tab

| Area                 | Property                                             | Description                                                                                                                       |
| -------------------- | ---------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------- |
| Export               | Include navigation panel settings in the export file | This property when activated exports the file along with Navigation Panel settings.                                               |
| License Registration | Unlock additional features                | You will be able to enter the ValQ license in the Text Box to get the latest features.     |
| Writeback | Write Scenarios, Planning series along with the Simulation details to an URL | This feature sends a post request to the endpoint URL in a JSON format and writes the data to an external data warehouse. |


