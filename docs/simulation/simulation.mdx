---
id: simulation
title: Simulate Tab
---
import useBaseUrl from '@docusaurus/useBaseUrl'; // Add to the top of the file below the front matter.
import Link from '@docusaurus/Link';

The Simulate tab has two panels in which the left panel represents the
Navigation Panel and the right panel denotes the canvas which represents
the Value Driver Tree visuals with different Nodes where the simulation
can be done at each Node Level (see Figure below).

<img alt="Simulate Tab option" src={useBaseUrl('/doc-images//simu1.png')} />

The common functionalities of the Simulate Tab has been already explained in <Link to={useBaseUrl('/docs/general/getting-started')}> `Section: General Features of valQ Custom Visual <GFV>`  </Link>